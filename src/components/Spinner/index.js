import React from 'react';

import './spinner.scss';

function Spinner() {
    return (
        <div className="spinner">
            <h1>LOADING...</h1>
        </div>
    );
}

export default Spinner;
